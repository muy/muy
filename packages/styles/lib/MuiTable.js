const MuiTable = theme => ({
  root: {
    borderCollapse: "collapse",
    fontSize: "1rem",
    lineHeight: `${theme.spacing(3)}px`,
    margin: `0 0 ${theme.spacing(3)}px`,
    padding: 0,
    width: "100%",
  },
})

export default MuiTable
