import React from "react"
import Box from "@material-ui/core/Box"

const Form = props => (
    <Box component="form"
         display="block"
         mb={3}
         mr={0}
         mt={0}
         p={0}
         {...props} />
)

export default Form
