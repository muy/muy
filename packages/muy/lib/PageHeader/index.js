import React from "react";
import Header from "../Header";

const PageHeader = (props) => <Header itemScope itemType="https://schema.org/WPHeader" role="banner" {...props} />

export default PageHeader
